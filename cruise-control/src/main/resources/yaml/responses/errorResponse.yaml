ErrorResponse:
  type: object
  required:
    - version
    - stackTrace
    - errorMessage
  properties:
    version:
      type: integer
      format: int32
    stackTrace:
      type: string
    errorMessage:
      type: string
