OperationProgress:
  type: object
  required:
    - version
    - operation
    - operationProgress
  properties:
    version:
      type: integer
      format: int32
    operation:
      type: string
      enum:
        - "Get customized proposals"
    operationProgress:
      type: array
      items:
        $ref: '../responses/stepProgress.yaml#/StepProgress'
