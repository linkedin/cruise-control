ProgressResult:
  type: object
  required:
    - version
    - progress
  properties:
    version:
      type: integer
      format: int32
    progress:
      $ref: '../responses/stepProgress.yaml#/StepProgress'
